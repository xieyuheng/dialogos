import * as Pattern from "../pattern"
import * as Node from "../node"
import * as ut from "../ut"

// export function from_node(node: Node.Node): Pattern.Pattern {
//   if (node) {
//   } else {
//   }
// }

// function parse_text(text: string): Pattern
